server:
  port: 9091

spring:
  application:
    name: distributor-${random.uuid}
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    publisher-confirm-type: correlated
    publisher-returns: true